[pytest]
# Only the lightweight smoke test runs in CI (installs pytest + numpy only).
# The full ML test suite (tests/evaluation, models, training, etc.) requires
# tensorflow / scikit-learn / scipy and is run separately with full deps.
testpaths = tests
python_files = test_*.py
addopts = -ra
